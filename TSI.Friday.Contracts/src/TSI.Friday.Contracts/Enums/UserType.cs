namespace TSI.Friday.Contracts.Enums
{
    /// <summary>
    /// Types of users/roles available in the system.
    /// Keep values as names so they map easily from string payloads.
    /// </summary>
    public enum UserType
    {
        Admin,
        User,
    }
}

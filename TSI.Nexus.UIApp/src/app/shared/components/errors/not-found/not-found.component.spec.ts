import { NotFoundComponent } from './not-found.component';

describe('NotFoundComponent', () => {
  it('should create the component when instantiated', () => {
    // Act / Assert
    expect(new NotFoundComponent()).toBeTruthy();
  });
});
